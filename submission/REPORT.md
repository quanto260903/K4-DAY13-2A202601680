# Báo cáo Day 13 Observability

## 1. Thông tin nhóm

- Tên nhóm:
- Repository URL:
- Commit SHA cuối:
- Thành viên và vai trò:

## 2. Kết quả kỹ thuật

- Điểm `validate_logs.py`: 100/100 (42 log records, 0 missing required fields, 0 missing enrichment, 20 unique correlation IDs)
- Tổng số traces: >= 10 (generated by `BASE_URL=http://127.0.0.1:8002 python scripts/load_test.py`)
- Số PII leak còn lại: 0
- Link/đường dẫn dashboard:

## 3. Logging và tracing

- Evidence correlation ID:
- Evidence PII redaction:
- Evidence trace waterfall: Langfuse trace waterfall should show parent `run` with child spans `retrieve` and `generate`.
- Giải thích một span đáng chú ý: `retrieve` captures the mock RAG lookup; `generate` captures fake LLM latency/token generation under the parent agent run.

## 4. Prompt versioning

- Prompt name:
- Version/label baseline:
- Version/label candidate:
- Trace ID của mỗi version:
- Bằng chứng đổi label hoặc rollback:

## 5. Dashboard, SLO và alerts

- Kết quả `validate_dashboard.py`: HOP LE: 6/6 panel co trong dashboard contract.
- Evidence dashboard: dashboard spec completed in `docs/dashboard-spec.md`; runtime fields come from `GET /metrics`.
- SLO đã chọn và lý do: latency P95 <= 3000 ms, error rate <= 2%, quality_avg >= 0.75, total_cost_usd <= 2.5, token budget <= 50000 to cover reliability, quality, and cost control.
- Alert rules và runbook: `config/alert_rules.yaml` defines `high_latency_p95`, `elevated_error_rate`, and `cost_budget_exceeded`; runbook details are in `docs/alerts.md`.

Câu hỏi phản biện: Alert rules nên symptom-based vì người dùng chỉ thấy chậm, lỗi hoặc bị giới hạn dịch vụ, không thấy tên hàm nội bộ. Cảnh báo dựa trên SLO/triệu chứng giúp ưu tiên đúng tác động người dùng và vẫn cho phép đổi implementation mà không phải viết lại toàn bộ alert.

## 6. Điều tra challenge

- Challenge ID:
- Triệu chứng từ metrics:
- Trace ID liên quan:
- Log line/correlation ID liên quan:
- Root cause:
- Fix action:
- Preventive measure:

## 7. Đóng góp cá nhân

Với mỗi thành viên, ghi rõ nhiệm vụ và link commit/PR tương ứng.

| Thành viên | Phần việc | Commit/PR | Điều đã học |
|---|---|---|---|
| | | | |
